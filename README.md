# Todo APP

> Todo app based on **ReactJS + Vite**, using simple `modular` architecture.

## **Basic scripts**

**Run project**:

```bash
npm run dev
```

**Build project**:

```bash
npm run build
```
